---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-issuer
  namespace: sqlite-operator-system
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sqlite-operator-serving-cert
  namespace: sqlite-operator-system
spec:
  # The dnsNames replacements below are patched by kustomize from the
  # webhook-service name and namespace.
  dnsNames:
    - sqlite-operator-webhook-service.sqlite-operator-system.svc
    - sqlite-operator-webhook-service.sqlite-operator-system.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: selfsigned-issuer
  secretName: webhook-server-cert
