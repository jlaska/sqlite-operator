{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "sqlite-operator.fullname" . }}-selfsigned
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sqlite-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "sqlite-operator.certName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sqlite-operator.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "sqlite-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "sqlite-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "sqlite-operator.fullname" . }}-selfsigned
  secretName: {{ include "sqlite-operator.webhookSecretName" . }}
{{- end }}
